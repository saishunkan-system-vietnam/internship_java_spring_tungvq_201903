<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssv.template.data.mapper.BuildingMapper">
	<insert id="insertBuilding" parameterType="Building" >
        INSERT INTO Building (NAME, NAME_JP, HEIGHT_M, HEIGHT_FT, FLOORS, YEAR, CITY, IMAGE) VALUES (#{name},#{nameJp},#{heightM},#{heightFt},#{floors},#{year},#{city},#{image});
    </insert>
 
    <update id="updateBuilding" parameterType="Building">
        UPDATE Building SET NAME = #{name} WHERE ID = #{id};
        UPDATE Building SET NAME_JP = #{nameJp} WHERE ID = #{id};
         UPDATE Building SET HEIGHT_M = #{heightM} WHERE ID = #{id};
          UPDATE Building SET HEIGHT_FT = #{heightFt} WHERE ID = #{id};
           UPDATE Building SET FLOORS = #{floors} WHERE ID = #{id};
            UPDATE Building SET YEAR = #{year} WHERE ID = #{id};
             UPDATE Building SET CITY = #{city} WHERE ID = #{id};
              UPDATE Building SET IMAGE = #{image} WHERE ID = #{id};
    </update>
     
    <delete id="deleteBuildingById" parameterType="int">
        DELETE from Building WHERE ID = #{id};
    </delete>
    
    <select id="selectBuildingById" parameterType="int">
    	SELECT * FROM Building WHERE ID = #{id};
    </select>
    
     
    <resultMap id = "result" type = "Building">
       <result property = "id" column = "ID"/>
       <result property = "name" column = "NAME"/>
    </resultMap>
	
</mapper>
